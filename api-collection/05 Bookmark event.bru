meta {
  name: 05 Bookmark event
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/me/bookmarks/{{eventId}}
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.bookmarked: eq true
}
